# LLM-to-VLA Curriculum — Python Dependencies
# Install: pip install -r requirements.txt
# Note: Some exercises use GPU; a CUDA-capable GPU is recommended but not required.

# Core ML
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0

# Transformers & LLM fine-tuning (Exercises 05, 08, 11)
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0
bitsandbytes>=0.43.0
datasets>=2.18.0
sentencepiece>=0.2.0

# RL & Control (Exercise 09)
gymnasium>=0.29.0
stable-baselines3>=2.3.0

# Robotics & Imitation Learning (Exercises 10, 11)
# lerobot: install from source — pip install git+https://github.com/huggingface/lerobot.git
simple-pid>=2.0.0

# 3D & Depth (Exercise 07)
open3d>=0.18.0            # optional — needed for point cloud visualization
scipy>=1.12.0

# Visualization & Logging
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
wandb>=0.16.0             # optional — for experiment tracking

# Diffusion (Exercise 09)
diffusers>=0.27.0

# Image handling
Pillow>=10.0.0
