# LLM-to-VLA Curriculum — Python Dependencies
# Install: pip install -r requirements.txt
# Note: Some exercises use GPU; a CUDA-capable GPU is recommended but not required.
# Note: bitsandbytes requires Linux + CUDA. On macOS/Windows, skip it or use CPU-only mode.

# Core ML
torch>=2.1.0
torchvision>=2.1.0
numpy>=1.24.0

# Transformers & LLM fine-tuning (Exercises 05, 08, 11)
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0
bitsandbytes>=0.43.0       # Linux + CUDA only
datasets>=2.18.0
sentencepiece>=0.2.0
tiktoken>=0.6.0             # BPE tokenization (Exercise 04)
trl>=0.8.0                  # DPO / PPO fine-tuning (Exercise 05)

# RL & Control (Exercise 09, Note 11)
gymnasium>=0.29.0
stable-baselines3>=2.3.0

# Robotics & Imitation Learning (Exercises 10, 11)
# lerobot: install from source — pip install git+https://github.com/huggingface/lerobot.git
# robomimic: install from source — pip install git+https://github.com/ARISE-Initiative/robomimic.git
simple-pid>=2.0.0
mujoco>=3.1.0               # Physics simulation (Exercise 10, 11)
einops>=0.7.0                # Tensor reshaping (used throughout)

# 3D, Depth & Video (Exercise 07)
open3d>=0.18.0              # optional — point cloud visualization
scipy>=1.12.0
opencv-python>=4.9.0        # Video processing (Exercise 07, 09)

# Vision (Exercise 06, 08)
open_clip_torch>=2.24.0     # CLIP / SigLIP models
supervision>=0.19.0         # Detection visualization
pycocotools>=2.0.7          # COCO evaluation metrics

# Visualization & Logging
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
wandb>=0.16.0               # optional — experiment tracking
tensorboard>=2.15.0         # optional — alternative to wandb
ipywidgets>=8.1.0           # Jupyter interactive widgets

# Diffusion (Exercise 09, Note 12)
diffusers>=0.27.0

# Image handling
Pillow>=10.0.0

# Sim-to-Real & Domain Randomization (Exercise 12, 13)
albumentations>=1.4.0       # Visual domain randomization
scikit-image>=0.22.0        # Image processing utilities

# Data science (Note 12 hands-on)
scikit-learn>=1.4.0
